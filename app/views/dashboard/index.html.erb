<h1>What's on Tap?</h1>

<div class="row">
  <% @beer_taps.each do |beer_tap| %>
    <%= render :partial => 'kegs/beer_tap', :locals => {:beer_tap => beer_tap} %>
  <% end %>
</div>

<% if current_user.admin? %>

<h1>To Order Next:</h1>

<div class="row well">
  <div class="span12">
    <%= @recommended.map{|beer| beer.name}.to_sentence %>
  </div>
</div>

<% end %>


<h1>&#9765; The Bible &#9765;</h1>

<%= render 'beer_list' %>